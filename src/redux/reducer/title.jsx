import Immutable from 'immutable';
import titleAction from '../action';

const defaultTitle = Immutable.fromJs({title: '系统管理', subTitle: '系统主页'});

const title = (state, action) => {

}

export default title;